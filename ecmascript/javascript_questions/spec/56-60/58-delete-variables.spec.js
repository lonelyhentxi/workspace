describe("delete variables", () => {
    it("global and let", () => {
        let name = "Lydia";
        age = 21;

        // 'delete name' can not be used in strict mode - global variables.
        // 'delete age' can not be used in strict mode - local variables.
    });
});
