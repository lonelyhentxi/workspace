describe("global typo", () => {
    it("create on global", () => {
        // eslint-disable-next-line no-unused-vars
        let greeting;
        greetign = {};
        expect(greetign).toEqual({});
    });
});
